{
  "name": "taskflow-monorepo",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix taskmanager",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix taskmanager"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}