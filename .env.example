# For local dev, Vite proxies /api to localhost:5000
# For production (Railway), set this to your Railway backend URL:
# VITE_API_URL=https://your-app.railway.app/api

# Leave empty for Railway (served from same origin):
VITE_API_URL=
